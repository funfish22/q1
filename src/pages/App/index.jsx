import { BrowserRouter } from 'react-router-dom';
import Router from './Router';

const AppPage = () => {
    return (
        <BrowserRouter>
            <Router />
        </BrowserRouter>
    );
};

export default AppPage;
